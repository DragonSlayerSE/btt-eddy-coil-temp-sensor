# btt-eddy-coil-temp-sensor
an attempt to add a internal temp sensor to btt eddy coil .. this is a log

efter att ha ändrar [temperatyre sensor] till [temperature probe] fick jag felmeddelandet Z_OFFSET_APPLY_PROBE already set.. jag kunde då radera eddy probe savet från printer.cfg för att komma förbi felmeddelandet

